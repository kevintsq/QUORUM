# Ablation preset -- keys are relative to pipeline.slam
# Flattened to Hydra CLI overrides by scripts/ablation_presets.py
ba:
  embedding_weight: 0.001
  kernel:
    mode: temporal
embedding_covisibility:
  enabled: true
loop_closure:
  enabled: true
pca_dim: 256
radseg_lang_align: true
radseg_slide_crop: 336
use_semantic_flow_init: true
