services:
  vipe:
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
    image: vipe:latest
    restart: unless-stopped
    network_mode: host
    ipc: host
    environment:
      - QT_X11_NO_MITSHM=1
      - NVIDIA_VISIBLE_DEVICES=GPU-17cec8e4-1239-7606-e979-1e2ccc260f1e    # <-- only use cuda:0
      - NVIDIA_DRIVER_CAPABILITIES=all
      - DISPLAY=$DISPLAY
      - VGL_DISPLAY=${RENDER_DISPLAY:-":1"}
    volumes:
      - /tmp/.X11-unix/:/tmp/.X11-unix
      - $ROOT_DIR/:/home/user/km-vipe
      - .cache/:/home/user/.cache/
      - .models_cache/:/root/.cache/
      - $DATA_DIR/:/data
    runtime: nvidia
    privileged: true
    command: bash

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0']       # <-- pin to GPU 1
              capabilities: [gpu]
